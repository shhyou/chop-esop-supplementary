#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using ex-04.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it)
(define-lump L)
(define-concolic-test
 ex-04
 #:inputs
 (|•(0 any/s any/c)|
  (or/s
   (->s integer integer)
   (list/s boolean (list-of integer))
   integer
   boolean))
 #:prop
 (prop-not-exn
  (λ ()
    (define (_f x) (if (number? x) (add1 x) (string-length x)))
    (define-id-with-ctc (-> (or/c string? number?) number?) _f f bug bug)
    (define (_g x) (f x))
    (define-id-with-ctc (-> any/c number?) _g g bug bug)
    (g
     (apply-ctc
      any/c
      (convert-it |•(0 any/s any/c)| any/s L)
      bad-input
      no-blame
      |argument of g|)))))
(define (counterexample)
  (define test-result (concolic-test ex-04 #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input ex-04 witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (ex-04 test-property)))
(module+ main (counterexample))
