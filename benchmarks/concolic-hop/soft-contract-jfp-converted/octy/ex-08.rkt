#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using ex-08.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it concolic-hop/complex)
(define-lump L)
(define-concolic-test
 ex-08
 #:inputs
 (|•(0 any/s any/c)|
  (or/s
   (->s
    (list/s integer integer integer integer)
    (list/s integer integer integer integer))
   (list/s integer integer integer integer)
   boolean))
 #:prop
 (prop-not-exn
  (λ ()
    (define (_strnum? x) (or (string? x) (c:integer? x)))
    (define-id-with-ctc
     (->i
      ((x any/c))
      (res
       (x)
       (and/c boolean? (λ (a) (equal? a (or (string? x) (c:number? x)))))))
     _strnum?
     strnum?
     bug
     bug)
    (strnum?
     (apply-ctc
      any/c
      (convert-it |•(0 any/s any/c)| any/s L #:arithmetic-coercion-both)
      bad-input
      no-blame
      |argument of strnum?|)))))
(define (counterexample)
  (define test-result (concolic-test ex-08 #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input ex-08 witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (ex-08 test-property)))
(module+ main (counterexample))
