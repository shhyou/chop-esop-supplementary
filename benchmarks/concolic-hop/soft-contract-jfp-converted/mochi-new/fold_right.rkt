#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using fold_right.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it)
(define-lump L)
(define-concolic-test
 fold_right
 #:inputs
 (|•(0 integer integer?)| integer)
 (|•(1 integer integer?)| integer)
 #:prop
 (prop-not-exn
  (λ ()
    (define (_fold_right f xs acc)
      (cond
       ((empty? xs) acc)
       (else (f (car xs) (_fold_right f (cdr xs) acc)))))
    (define (_make_list n) (if (< n 0) empty (cons n (_make_list (- n 1)))))
    (define (_add x y) (+ x y))
    (define (_main n m)
      (let ((xs (_make_list n))) (> (_fold_right _add xs m) m)))
    (define-id-with-ctc
     (-> integer? integer? (not/c false?))
     _main
     main
     bug
     bug)
    (main
     (apply-ctc
      integer?
      (convert-it |•(0 integer integer?)| integer L)
      bad-input
      no-blame
      |argument of main|)
     (apply-ctc
      integer?
      (convert-it |•(1 integer integer?)| integer L)
      bad-input
      no-blame
      |argument of main|)))))
(define (counterexample)
  (define test-result (concolic-test fold_right #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input fold_right witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (fold_right test-property)))
(module+ main (counterexample))
