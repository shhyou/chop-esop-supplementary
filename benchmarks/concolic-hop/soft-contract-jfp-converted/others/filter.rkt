#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using filter.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it)
(define-lump L)
(define-concolic-test
 filter
 #:inputs
 (|•(0 (->s any/s any/s) (-> any/c any/c))|
  (->s
   (or/s
    (->s integer integer)
    (list/s boolean (list-of integer))
    integer
    boolean)
   (or/s
    (->s integer integer)
    (list/s boolean (list-of integer))
    integer
    boolean)))
 (|•(1 (list-of any/s) (listof any/c))|
  (list-of
   (or/s
    (->s integer integer)
    (list/s boolean (list-of integer))
    integer
    boolean)))
 #:prop
 (prop-not-exn
  (λ ()
    (define (_filter p? xs)
      (cond
       ((empty? xs) empty)
       (else
        (let ((x (car xs)) (zs (_filter p? (cdr xs))))
          (if (p? x) (cons x zs) zs)))))
    (define-id-with-ctc
     (->i
      ((p? (-> any/c any/c)) (xs (listof any/c)))
      (res
       (p? xs)
       (and/c (listof any/c) (λ (ys) (equal? (empty? xs) (empty? ys))))))
     _filter
     filter
     bug
     bug)
    (filter
     (apply-ctc
      (-> any/c any/c)
      (convert-it
       |•(0 (->s any/s any/s) (-> any/c any/c))|
       (->s any/s any/s)
       L)
      bad-input
      no-blame
      |argument of filter|)
     (apply-ctc
      (listof any/c)
      (convert-it |•(1 (list-of any/s) (listof any/c))| (list-of any/s) L)
      bad-input
      no-blame
      |argument of filter|)))))
(define (counterexample)
  (define test-result (concolic-test filter #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input filter witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (filter test-property)))
(module+ main (counterexample))
