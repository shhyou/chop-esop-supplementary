#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using factorial-acc.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it)
(define-lump L)
(define-concolic-test
 factorial-acc
 #:inputs
 (|•(0 integer integer?)| integer)
 #:prop
 (prop-not-exn
  (λ ()
    (define (_factorial n) (_factorial-acc n 1))
    (define (_factorial-acc n acc)
      (if (zero? n) acc (_factorial-acc (sub1 n) (* n acc))))
    (define-id-with-ctc
     (->i ((x integer?)) (res (x) (and/c integer? (>/c 1))))
     _factorial
     factorial
     bug
     bug)
    (factorial
     (apply-ctc
      integer?
      (convert-it |•(0 integer integer?)| integer L)
      bad-input
      no-blame
      |argument of factorial|)))))
(define (counterexample)
  (define test-result (concolic-test factorial-acc #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input factorial-acc witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (factorial-acc test-property)))
(module+ main (counterexample))
