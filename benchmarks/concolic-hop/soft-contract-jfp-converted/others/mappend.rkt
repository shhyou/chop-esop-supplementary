#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using mappend.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it)
(define-lump L)
(define-concolic-test
 mappend
 #:inputs
 (|•(0 (->s any/s (list-of any/s)) (-> any/c (listof any/c)))|
  (->s
   (or/s
    (->s integer integer)
    (list/s boolean (list-of integer))
    integer
    boolean)
   (list-of
    (or/s
     (->s integer integer)
     (list/s boolean (list-of integer))
     integer
     boolean))))
 (|•(1 any/s any/c)|
  (or/s
   (->s integer integer)
   (list/s boolean (list-of integer))
   integer
   boolean))
 #:prop
 (prop-not-exn
  (λ ()
    (define (_append xs ys)
      (if (empty? xs) ys (cons (car xs) (_append (cdr xs) ys))))
    (define (_map-append f xs)
      (if (empty? xs) empty (_append (f (car xs)) (_map-append f (cdr xs)))))
    (define-id-with-ctc
     (-> (-> any/c (listof any/c)) any/c (listof any/c))
     _map-append
     map-append
     bug
     bug)
    (define-id-with-ctc
     (-> (listof any/c) (listof any/c) (listof any/c))
     _append
     append
     bug
     bug)
    (map-append
     (apply-ctc
      (-> any/c (listof any/c))
      (convert-it
       |•(0 (->s any/s (list-of any/s)) (-> any/c (listof any/c)))|
       (->s any/s (list-of any/s))
       L)
      bad-input
      no-blame
      |argument of map-append|)
     (apply-ctc
      any/c
      (convert-it |•(1 any/s any/c)| any/s L)
      bad-input
      no-blame
      |argument of map-append|)))))
(define (counterexample)
  (define test-result (concolic-test mappend #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input mappend witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (mappend test-property)))
(module+ main (counterexample))
