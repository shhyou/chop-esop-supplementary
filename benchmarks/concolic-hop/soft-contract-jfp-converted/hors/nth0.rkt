#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using nth0.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it)
(define-lump L)
(define-concolic-test
 nth0
 #:inputs
 (|•(0 integer integer?)| integer)
 #:prop
 (prop-not-exn
  (λ ()
    (define (_nth n xs) (if (= n 1) (car xs) (_nth (- n 1) (cdr xs))))
    (define (_mk-list n) (if (< n 0) empty (cons n (_mk-list (- n 1)))))
    (define (_main n) (let ((xs (_mk-list n))) (if (empty? xs) 0 (_nth 0 xs))))
    (define-id-with-ctc
     (-> integer? (listof integer?) integer?)
     _nth
     nth
     bug
     bug)
    (define-id-with-ctc
     (-> integer? (listof integer?))
     _mk-list
     mk-list
     bug
     bug)
    (define-id-with-ctc (-> integer? integer?) _main main bug bug)
    (main
     (apply-ctc
      integer?
      (convert-it |•(0 integer integer?)| integer L)
      bad-input
      no-blame
      |argument of main|)))))
(define (counterexample)
  (define test-result (concolic-test nth0 #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input nth0 witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (nth0 test-property)))
(module+ main (counterexample))
