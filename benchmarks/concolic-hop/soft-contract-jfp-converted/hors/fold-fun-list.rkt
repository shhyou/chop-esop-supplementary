#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using fold-fun-list.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it)
(define-lump L)
(define-concolic-test
 fold-fun-list
 #:inputs
 (|•(0 (->s (->s integer (->s integer integer)) dont-care/s) (-> (->i ((n integer?)) (res (n) (and/c (-> integer? integer?) (λ (f) (>= (f 0) 0))))) dont-care/c))|
  (->s (->s integer (->s integer integer)) boolean))
 #:prop
 (prop-not-exn
  (λ ()
    (define (_mk-list n)
      (if (<= n 0) empty (cons (λ (m) (- m n)) (_mk-list (- n 1)))))
    (define (_foldr f z xs)
      (if (empty? xs) z (f (car xs) (_foldr f z (cdr xs)))))
    (define (_compose f g) (λ (x) (f (g x))))
    (define (_main n) (let ((xs (_mk-list n))) (_foldr _compose (λ (x) x) xs)))
    (define-id-with-ctc
     (-> integer? (listof (-> integer? integer?)))
     _mk-list
     mk-list
     bug
     bug)
    (define-id-with-ctc
     (->
      (-> (-> integer? integer?) (-> integer? integer?) (-> integer? integer?))
      (-> integer? integer?)
      (listof (-> integer? integer?))
      (-> integer? integer?))
     _foldr
     foldr
     bug
     bug)
    (define-id-with-ctc
     (->i
      ((n integer?))
      (res (n) (and/c (-> integer? integer?) (λ (f) (>= (f 0) 0)))))
     _main
     main
     bug
     bug)
    ((apply-ctc
      (->
       (->i
        ((n integer?))
        (res (n) (and/c (-> integer? integer?) (λ (f) (>= (f 0) 0)))))
       dont-care/c)
      (convert-it
       |•(0 (->s (->s integer (->s integer integer)) dont-care/s) (-> (->i ((n integer?)) (res (n) (and/c (-> integer? integer?) (λ (f) (>= (f 0) 0))))) dont-care/c))|
       (->s (->s integer (->s integer integer)) dont-care/s)
       L)
      bad-input
      no-blame
      |wrap main|)
     main))))
(define (counterexample)
  (define test-result (concolic-test fold-fun-list #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input fold-fun-list witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (fold-fun-list test-property)))
(module+ main (counterexample))
