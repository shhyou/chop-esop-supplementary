#lang concolic-hop/lang
;; this file generated by convert-soft-contract.rkt using sum-acm-e.sch as input
(require concolic-hop/ctc concolic-hop/lib concolic-hop/convert-it)
(define-lump L)
(define-concolic-test
 sum-acm-e
 #:inputs
 (|M(assert assert (->s any/s any/s) (-> (not/c false?) any/c))|
  (->s
   (or/s
    (->s integer integer)
    (list/s boolean (list-of integer))
    integer
    boolean)
   (or/s
    (->s integer integer)
    (list/s boolean (list-of integer))
    integer
    boolean)))
 (Amb integer)
 (|B(0 assert assert (->s (->s any/s any/s) dont-care/s) (-> (-> (not/c false?) any/c) dont-care/c))|
  (->s
   (->s
    (or/s
     (->s integer integer)
     (list/s boolean (list-of integer))
     integer
     boolean)
    (or/s
     (->s integer integer)
     (list/s boolean (list-of integer))
     integer
     boolean))
   boolean))
 (|B(1 main m (->s (->s any/s) dont-care/s) (-> (-> any/c) dont-care/c))|
  (->s
   (->s
    integer
    (or/s
     (->s integer integer)
     (list/s boolean (list-of integer))
     integer
     boolean))
   boolean))
 #:prop
 (prop-not-exn
  (λ ()
    (define _assert
      (convert-it
       |M(assert assert (->s any/s any/s) (-> (not/c false?) any/c))|
       (->s any/s any/s)
       L))
    (define-id-with-ctc (-> (not/c false?) any/c) _assert assert bad-input bug)
    (define (_sum x y k) (if (<= x 0) (k y) (_sum (- x 1) (+ x y) k)))
    (define (_check x) (assert (<= 100 x)))
    (define (_main) (_sum 5 0 _check))
    (define-id-with-ctc (-> any/c) _main main bug bug)
    (cond
     ((= Amb 0)
      ((apply-ctc
        (-> (-> (not/c false?) any/c) dont-care/c)
        (convert-it
         |B(0 assert assert (->s (->s any/s any/s) dont-care/s) (-> (-> (not/c false?) any/c) dont-care/c))|
         (->s (->s any/s any/s) dont-care/s)
         L)
        bad-input
        no-blame
        |wrap assert|)
       assert))
     (else
      ((apply-ctc
        (-> (-> any/c) dont-care/c)
        (convert-it
         |B(1 main m (->s (->s any/s) dont-care/s) (-> (-> any/c) dont-care/c))|
         (->s (->s any/s) dont-care/s)
         L)
        bad-input
        no-blame
        |wrap main|)
       main))))))
(define (counterexample)
  (define test-result (concolic-test sum-acm-e #:all? #f #:statistics? #t))
  (define witness (list-ref test-result 0))
  (define stats (list-ref test-result 1))
  (vector
   (concretize-input sum-acm-e witness)
   '()
   `#hash((solve-count . ,(concolic-statistics-solve-count stats))
          (solve-real-nongc-time
           .
           ,(concolic-statistics-solve-real-nongc-time stats))
          (test-count . ,(concolic-statistics-test-count stats)))))
(provide counterexample (rename-out (sum-acm-e test-property)))
(module+ main (counterexample))
